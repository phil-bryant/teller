# run sql unit tests Requirements

## Scope

Applies to `tests/t06_run_sql_unit_tests.sh`.

R005  Statement: Execute only the SQL unit-test lane.
Design: Implemented inline in `tests/t06_run_sql_unit_tests.sh` at the `#R005` tagged block.
Tests:
- R005-T01: Verify `tests/t06_run_sql_unit_tests.sh` carries the `#R005` implementation tag.
