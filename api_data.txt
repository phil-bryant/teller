api_data can only be either:
  a list of dicts
  a dict

all of the above dicts will always have a string key
all of the above dicts will have a value that can only be one of:
    a list of dicts
    a dict
    a string

and all of the above dicts have the same key/value definition as above,
in other words, the definition above is recursive.